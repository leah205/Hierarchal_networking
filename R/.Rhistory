edge.color = rgb(0.3, 0.3, 0.3, transp))
+ legend()
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(10)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("topright",
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(10)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("topright",
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topleft",
legend = c("Adult", "Subadult"), vertex.size = c(12, 6))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(10)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("topright",
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topleft",
legend = c("Adult", "Subadult"), pch = 19, pt.cex = c(12, 6))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(10)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("topright",
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topleft",
legend = c("Adult", "Subadult"), pch = 19, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(10)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("topright",
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topleft",
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(10)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("topright",
title = "Sex",
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topleft",
title = "Age",
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(10)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("topright",
title = "Sex",
inset = 0.1,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topleft",
title = "Age",
inset = 0.1,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(10)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("top",
title = "Sex",
inset = 0.1,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("bottom",
title = "Age",
inset = 0.1,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(10)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("topleft",
title = "Sex",
inset = 0.2,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topright",
title = "Age",
inset = 0.2,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(11)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("topleft",
title = "Sex",
inset = 0.15,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topright",
title = "Age",
inset = 0.15,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(11)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("bottomright",
title = "Sex",
inset = 0.15,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topright",
title = "Age",
inset = 0.15,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(11)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("bottomright",
title = "Sex",
inset = 0.15,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topright",
title = "Age",
inset = 0.05,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(11)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("bottomright",
title = "Sex",
inset = 0.08,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topright",
title = "Age",
inset = 0.15,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(2, 1))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(11)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 6),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.border = 0,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("bottomright",
title = "Sex",
inset = 0.08,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topright",
title = "Age",
inset = 0.15,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(4, 2))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(11)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 8),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.border = 0,
vertex.frame.width = 2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("bottomright",
title = "Sex",
inset = 0.08,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topright",
title = "Age",
inset = 0.15,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(4, 2))
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(11)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 8),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.border = 0,
vertex.frame.width = 2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("bottomright",
title = "Sex",
inset = 0.08,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topright",
title = "Age",
inset = 0.15,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(4, 2))
arrows(x0 = -1, y0 = 1,
x1 = -1, y1 = -1,
length = 0.1, lwd = 2)
text(x = -1, y = 1.2, labels = "Decreasing rank")
# A <- A[, -1]
# B <- B[, -1]
# C <- C[, -1]
set.seed(11)
network <- graph_from_adjacency_matrix(convert_to_mat(A), mode = "directed", weighted = TRUE)
#layout <- layout_as_tree(network, root = 10)
layout <- layout_with_fr(network)
rank_scaled <- scale(ranksA[, 2])
layout[,2] <- rank_scaled
#weight_range <- max(E(network)$weight) - min(E(network)$weight)
weight_factor = max(E(network)$weight) / 0.8
transp <- (0.2 + E(network)$weight / weight_factor)
plot(network,
layout = layout,
vertex.label.cex = 0.7,
vertex.size = ifelse(demo_df$adult, 12, 8),
edge.width = E(network)$weight / 10,
edge.arrow.size = 0.2,
vertex.border = 0,
vertex.frame.width = 2,
vertex.color = ifelse(demo_df$male, "#DCDBFF", "#FFDBF6"),
edge.color = rgb(0.3, 0.3, 0.3, transp))
legend("bottomright",
title = "Sex",
inset = 0.08,
legend = c("Male", "Female"), col = c('#DCDBFF', "#FFDBF6"),
lwd = 3)
legend("topright",
title = "Age",
inset = 0.15,
legend = c("Adult", "Subadult"), pch = 21, pt.cex = c(4, 2))
edge_mask <- which(weighted_edges$ind1 < weighted_edges$ind2)
weight_factor = max(weighted_edges$weight) / 0.8
transp <- (0.2 + weighted_edges$weight / weight_factor)
View(weighted_edges)
edges <- as.matrix(weighted_edges[,3:4])
edge_color <- ifelse(weighted_edges$ind1 < weighted_edges$ind2, rgb(0.75, 0.8, 1, transp), rgb(1, 0.25, 0.25, transp))
ranked_names <- rev(ranksA[[1]])
arcplot(edges, las=2,font = 1, col.arcs = edge_color,
above = above_below(edges, edge_mask), ordering = ranked_names, horizontal = F,
lwd.arcs = 2, pch.nodes = 19, sorted = T, decreasing = F
)
legend("topright",
title = "Dominant Node in Interaction",
legend = c("Higher ranked", "Lower ranked"), col = c(rgb(0.75, 0.8, 1, 1), rgb(1, 0.25, 0.25, 1)),
lwd = 3)
create_table <- function(df){
ranksTableA <- df
ranksTableA$ranks <- round(ranksTableA$ranks, 3)
colnames(ranksTableA) <- c("Name", "Rank")
df_t <- as.data.frame(t(ranksTableA), stringsAsFactors = F)
df_t %>% kable(format = "html") %>% kable_styling() %>%
column_spec(1:ncol(df_t), border_left = TRUE, border_right = TRUE) %>%
row_spec(nrow(df_t), extra_css = "border-bottom: 1px solid black;")
}
create_table(ranksA[1:floor(nrow(ranksA) / 2), ])
create_table(ranksA[floor(nrow(ranksA) / 2 + 1):nrow(ranksA),])
create_table(ranksB[1:floor(nrow(ranksB) / 2), ])
create_table(ranksB[floor(nrow(ranksB) / 2 + 1):nrow(ranksB),])
create_table(ranksB[1:floor(nrow(ranks ) / 2), ])
create_table(ranksB[floor(nrow(ranksB) / 2 + 1):nrow(ranksB),])
